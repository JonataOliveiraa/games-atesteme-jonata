/**
 * ═══════════════════════════════════════════════════════════════════════
 *  APOSENTADA — todo o desenho passou para a GameScene
 * ═══════════════════════════════════════════════════════════════════════
 *
 * Esta cena desenhava o topo: a faixa, o nível, o enunciado e os indicadores.
 * Para isso ela precisava de canais de mensagem com a cena de jogo só para
 * mover números entre duas cenas que sempre viveram juntas — e o enunciado não
 * tinha como saber onde o relógio estava, porque estavam em arquivos
 * diferentes.
 *
 * O arquivo continua aqui só porque esta sessão não consegue apagar arquivos no
 * computador. **Pode ser deletado** — nada o importa: `index.ts` registra
 * apenas `[BootScene, GameScene]`.
 */
export {}
